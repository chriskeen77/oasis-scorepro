---
description: Load context from the Keep — who Chris is and what's in flight
---

Orient yourself from the Keep — Chris's personal memory system at
`$KEEP_DIR` if that environment variable is set, otherwise `~/keep`. If
neither exists, say so and stop.

1. Read `index.md`.
2. Read `profile.md`.
3. If the current working directory matches a project in `projects/`, read
   that file. If Chris named a topic after the command (e.g.
   `/recall oasis`), read the matching project file instead.
4. Read the 2–3 newest files in `journal/` for recent context.

Then give Chris a short brief — a few sentences, not a report: what's in
flight, where we left off on the relevant project, and anything recent worth
remembering. End by asking nothing; just be oriented and ready.
